// Fills in sub-project #7 (Feedback). The /admin layout's <AdminNav/> will
// gain an "Open Flags" tab there, and this module will export listFlags() /
// countOpenFlags() / resolveFlag() against act.question_flags.
export {};
